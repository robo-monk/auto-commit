{
  "name": "auto-commit",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build index.ts --outfile auto-commit --target bun --minify --compile --bytecode"
  },
  "bin": {
    "auto-commit": "./auto-commit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "tiktoken": "^1.0.18"
  }
}